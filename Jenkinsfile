pipeline {
    agent any
stages {
        stage('build') {
            steps {

                echo "clone successfull"
                
            
            }
        }
        }

post {
        always {
            echo "running build"
        }
        success{
            echo "finish building"
        }
    }
}
